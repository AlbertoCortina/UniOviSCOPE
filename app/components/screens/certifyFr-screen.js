/**
 * Certify FR Screen
 *
 * Pantalla para el reconocimiento facial de la sesión.
 */
import React from 'react'
import {View, ScrollView,} from 'react-native'
import {Item, Input, Label, Text, Button} from 'native-base'
import {Avatar} from 'react-native-elements'
import I18n from '../../resources/i18n'
import ActivityIndicator from '../custom/custom-activityIndicator'
import {profileStyles as styles} from '../../resources/styles'

class CertifyFrScreen extends React.Component {

    render() {
        return (
           <View/>
        )
    }
}

export default CertifyFrScreen