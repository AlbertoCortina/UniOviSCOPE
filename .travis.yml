language:
  node_js
branches:
  only:
  - master
cache:
  directories:
  - node_modules
before_install:
- npm update
install:
- npm install
- npm install -g codecov
script:
- npm test-coverage
- codecov
